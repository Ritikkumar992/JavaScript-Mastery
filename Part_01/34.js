// for in loop: it gives the index of the array.

let fruits = ["apple","grapes","oranges"];

for(let i in fruits){
    console.log(i); // it gives the index of the array.
    console.log(fruits[i]); // it prints the item also,
}